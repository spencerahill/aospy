name: test_env
channels:
  - conda-forge
dependencies:
  - python=3.4
  - scipy
  - netCDF4
  - xarray
  - dask
  - pytest
  - future
  - matplotlib
  - ipython
  - pip:
    - coveralls
    - multiprocess
    - pytest-cov
    - pytest-catchlog
    - runipy
